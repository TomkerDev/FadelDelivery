generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

// Un modèle de test pour vérifier que ça marche
model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}